# Cloudflare Worker (GraphQL Backend)

Handles GraphQL queries and mutations for sensor readings.

- Deployed via Cloudflare Workers
- Uses D1 database

Note: Environment bindings and secrets are not included.